<div class="container">
  <h2>Altere sua senha</h2>
  <br>

  <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>

    <%= f.input :reset_password_token, as: :hidden %>
    <%= f.full_error :reset_password_token %>

    <div class="form-inputs">
      <%= f.input :password, label: "Nova senha", required: true, autofocus: true, :input_html => {:style => 'width: 400px'}, hint: ("número mínimo de caracteres: #{@minimum_password_length}" if @minimum_password_length) %>
      <%= f.input :password_confirmation, label: "Confirmar nova senha", required: true, :input_html => {:style => 'width: 400px'} %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Altere sua senha" %>
    </div>
  <% end %>
</div>
<%= render "devise/shared/links" %>

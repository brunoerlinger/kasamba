#SHOW | My_product

<h2> <%= @my_product.name %></h2>
<h2><%= @my_product.description %></h2>

<%= link_to "Edit", edit_my_product_path(@my_product), class:"btn btn-primary" %>

<%= link_to "Delete", my_product_path(@my_product), class:"btn btn-danger", method: :delete, data: { confirm: "Are you sure?" } %>
